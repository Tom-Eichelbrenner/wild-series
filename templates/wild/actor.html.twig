{% extends 'base.html.twig' %}

{% block title %}{{ name }}{% endblock %}

{% block body %}
    {#    <h1>Fiche de l'acteur {{ name }} </h1>#}
    {#    <h2> Programs : </h2>#}

    {#    {% for program in programs %}#}
    {#        <div>#}
    {#            <ul>#}
    {#                <li><h2>#}
    {#                        <a href="{{ path('wild_show', { 'slug': program.title|replace({' ': '-'})|lower }) }}"> {{ program.title }}</a>#}
    {#                    </h2>#}
    {#                </li>#}
    {#            </ul>#}
    {#        </div>#}
    {#    {% endfor %}#}


    <div class="container-fluid">
        <div class="row">
            <div class="col-8 mx-auto">
                <div class="card mx-auto" style="width: 18rem; margin-top: 25vh">
                    <div class="card-body">
                        <h5 class="card-title">{{ name }}</h5>
                        <p class="card-text">Cet acteur a joué dans :{% for program in programs %} <a
                                href="{{ path('wild_show', { 'slug': program.title|replace({' ': '-'})|lower }) }}">
                            <br>
                            - {{ program.title }}</a>
                            {% endfor %}
                            et bien d'autres..
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}