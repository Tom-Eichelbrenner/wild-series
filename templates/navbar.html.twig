<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-primary">
        <a class="navbar-brand primary text-fifth" href="{{ path('wild_index') }}">Wild Series</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link text-fifth" href="/">Home <span class="sr-only">(current)</span></a>
                    <a class="nav-link text-fifth" href="{{ path('program_index') }}">All programs <span class="sr-only">(current)</span></a>
                </li>
                {% if app.user %}
                    {{ app.user.email }}
                    <a href="{{ path('app_logout') }}"> Se déconnecter</a>
                    <a href="{{ path('my-profile') }}">Mon compte</a>
                {% else %}
                    <a href="{{ path('app_login') }}"> Se connecter</a>
                {% endif %}

            </ul>
        </div>
    </nav>
</header>